# PaperQuay 用户手册

> 适用版本：**v0.3.0**（笔记章节主体对应 v0.2.0 的笔记系统改造，并已同步 v0.2.1–v0.3.0 的行为变化）。
> 更新日期：2026-09-26。
> 相关文档：[README](../README.md)（项目概览与能力清单）、[笔记宪章](./notes-charter.md)（页面类型、命名规范、摘录卡红线）、[MCP 接入指南](./MCP_AGENT_INTEGRATION.md)、[开发手册](./DEVELOPMENT.zh-CN.md)。

本手册面向**使用者**：怎么导入论文、怎么读、怎么写笔记、怎么把笔记维护成一个长期可用的知识体系。开发、构建、上游同步和发布流程见[开发手册](./DEVELOPMENT.zh-CN.md)。

## 目录

- [1. 安装与首次启动](#1-安装与首次启动)
- [2. 文献库](#2-文献库)
- [3. 阅读器](#3-阅读器)
- [4. 笔记：使用](#4-笔记使用)
- [5. 笔记：维护](#5-笔记维护)
- [6. Agent 工作区](#6-agent-工作区)
- [7. 知识图谱](#7-知识图谱)
- [8. 综述写作](#8-综述写作)
- [9. 本地 RAG 知识库](#9-本地-rag-知识库)
- [10. MCP 与外部 Agent 接入](#10-mcp-与外部-agent-接入)
- [11. 数据、备份与隐私](#11-数据备份与隐私)
- [12. 设置参考](#12-设置参考)
- [13. 更新与排障](#13-更新与排障)
- [附录 A：快捷键](#附录-a快捷键)
- [附录 B：常见问题速查](#附录-b常见问题速查)

---

## 1. 安装与首次启动

### 1.1 安装

1. 从项目的 GitHub Releases 页面下载对应平台的安装包（Windows 提供 `-win-x64` 安装程序；macOS 提供手动下载的发布页；Linux 提供自动更新包）。
2. 安装后首次启动，应用会自动在系统的用户数据目录下创建 PaperQuay 数据目录，不需要额外初始化步骤。
3. 从源码运行（开发者）要求 Node.js 18+，使用 `npm ci` 安装依赖、`npm run dev` 启动桌面开发模式；这部分不属于本手册范围。

### 1.2 首次启动建议按顺序配置

打开左侧栏的「设置」，按下面的顺序配置，后面所有功能都依赖它：

| 顺序 | 设置分区 | 要做的事 | 不配置的后果 |
| --- | --- | --- | --- |
| 1 | AI 模型 | 填入 OpenAI 兼容接口的 Base URL、API Key，并添加模型预设 | 翻译、概览、问答、Agent、笔记润色、区块重析全部不可用 |
| 2 | 文库与 Zotero | 指定文献存储目录、导入模式（复制/移动/保留原路径）、文件命名规则 | 导入 PDF 时会使用默认数据目录下的 `paperquay-data` |
| 3 | 文档解析 | 填 MinerU API Token（默认引擎）；需要时再填 PaddleOCR-VL Token | 无法解析结构化正文，阅读器只能看原始 PDF |
| 4 | AI 模型（Embedding） | 配置 Embedding 端点、模型与维度 | 本地 RAG 与笔记检索都退化为关键词检索，笔记润色的文献引用不可用 |
| 5 | 备份 / WebDAV | 可选；配置远端后手动备份 | 数据只在本机 |
| 6 | 概览与问答 | 可选；调整概览输入、批量概览、问答上下文 | 使用默认策略 |

### 1.3 数据放在哪里

PaperQuay 是本地优先应用，数据都在本机：

| 位置 | 内容 |
| --- | --- |
| `<数据目录>/PaperQuay/paperquay-library.sqlite` | 文献库：文献元数据、分类、标签、批注、阅读记录、文库设置（含笔记 vault 目录配置） |
| `<数据目录>/PaperQuay/paperquay-notes.sqlite` | 笔记库：笔记正文、标签、分类（文件夹）、双链、锚点、参考文献关联 |
| `<数据目录>/PaperQuay/paperquay-rag.sqlite` | RAG 索引：切片、FTS5 虚表、向量索引、Agent 运行记录 |
| `<数据目录>/PaperQuay/.settings/paperquay.config.json` | 界面与功能设置、模型预设、密钥 |
| `<数据目录>/PaperQuay/agent-memory/` | Agent 三层记忆文件 |
| `<数据目录>/PaperQuay/.mineru-cache/`、`.downloads/`、`.screenshots/` | 解析缓存、远程 PDF 下载、截图 |
| 用户配置的文献存储目录 | 入库的 PDF 文件 |
| 用户配置的笔记 vault 目录（可选） | 笔记的 Markdown 镜像，可直接用 Obsidian 打开 |

Windows 上数据目录通常是 `%APPDATA%/PaperQuay`，macOS 是 `~/Library/Application Support/PaperQuay`，Linux 是 `~/.config/PaperQuay`。

### 1.4 界面总览

应用以「工作区标签页」组织：

- **Library（文献库）**：文献列表、分类树、详情面板、批量操作。
- **Reader（阅读器）**：PDF 阅读、结构块视图、翻译、批注、文档问答、摘录与笔记。
- **Notes（笔记）**：独立笔记工作区，含编辑器、分类、图谱与体检。
- **Agent**：对话式文献操作与调研。
- **Graph（知识图谱）**：文献、笔记、标签、分类与引用关系。
- **Review（综述写作）**：大纲蓝图、分段写作、Word 导出。
- 打开笔记会生成独立的笔记标签页，可以同时打开多篇笔记。

---

## 2. 文献库

### 2.1 导入 PDF

1. 在文献库拖入 PDF 文件，或使用导入按钮选择文件。
2. 进入**导入确认窗口**：应用会尝试补全元数据（OpenAlex 优先、Crossref 兜底；中文论文远程未命中时用大模型从首页文本提取），你可以逐条修改标题、作者、年份、期刊、DOI 等，然后再确认入库。
3. 入库时按设置执行文件处理：复制到文献存储目录 / 移动 / 保留原路径；重复文件按内容哈希去重。

### 2.2 从 Zotero 导入

- 在「设置 → 文库与 Zotero」指定 Zotero 数据目录（应用读取 `zotero.sqlite` 的临时副本，不修改原库）。
- 支持两种方式：按分类全量导入，或**选择性同步**——浏览分类树、按关键词/作者/年份检索候选文献，先做差量预检（已存在 / 缺本地 PDF / 可导入），确认后再入库。
- 导入会保留 Zotero 的分类与标签结构，作为普通文献进入 PaperQuay 文库，之后两者互不依赖。

### 2.3 分类、标签与收藏

- **分类树**：系统分类（所有文献 / 最近导入 / 未分类 / 收藏）+ 自定义分类与任意层级子分类；支持折叠、右键菜单、拖拽排序与层级调整。
- **标签**：在文献详情里维护，可多选筛选。
- **收藏**：文献详情或列表中的星标。
- 分类是「归属」，标签是「横切维度」——一篇文献只在一个位置，但可以有很多标签。这个约定和笔记一致。

### 2.4 元数据与批量操作

- 文献详情面板可编辑全部元数据字段，并显示阅读时间图、笔记与引用。
- 列表支持多选（Ctrl/Shift 勾选，或一键全选当前筛选结果，跨页有效），批量执行：删除、移动分类、收藏、批量翻译标题、导出 Bib。
- **Bib 导出**：多选后可合并为单个 `.bib` 或每篇一个文件，自动生成去重 citation key，覆盖期刊、书籍、学位论文、报告等条目类型。

### 2.5 阅读进度

阅读器会自动记录阅读时间与滚动位置，在文献详情里以热力图和进度形式呈现，方便回看「哪篇读到哪了」。

---

## 3. 阅读器

### 3.1 PDF 阅读与目录

- 打开文献进入阅读器，左侧是 PDF 正文，右侧是可停靠的助手侧栏。
- **PDF 目录侧栏**：树状折叠、关键词筛选、当前阅读页章节自动高亮；文档自带目录时直接读取，缺失时用 MinerU 章节索引即时生成。
- 长文档按视口窗口化渲染结构块与缩略图，避免几百页论文滚动卡顿。

### 3.2 结构块视图与 PDF 区域联动

- 解析完成后可切换到 MinerU 结构块视图：正文块、公式、表格、图片按结构呈现，与 PDF 页面区域双向联动（点块跳页、点页定位块）。
- 解析失败的公式或表格可以**回退到 PDF 原始切片（BBox Crop）**显示，也可以对任意块执行**区块 AI 重析**：排版纠错、表格/术语表结构化、数学公式提取，支持对比预览与撤销。
- 重析结果作为本地区块修复保存，按解析版本隔离，不改写原始 OCR 数据；清空修复即回到原始解析。

### 3.3 解析引擎与重新识别

- 「设置 → 文档解析」可选引擎：**MinerU（默认）** 与 **PaddleOCR-VL**；两者输出统一的缓存契约，下游功能不受影响。
- 支持多个 API Key 的轮换调度与故障切换；超页大文件自动按页数切片分卷、本地合并（页码校准、图片命名空间隔离）。
- 支持强制重新识别单篇文献，以及解析任务的状态与阶段进度显示（任务在后台继续，不依赖阅读页保持打开）。

### 3.4 翻译

- **全文翻译**：按结构块翻译并缓存，可随时切换原文/译文对照；译文 PDF 可集中存放与迁移。
- **划词翻译**：选中文本后浮层显示翻译，可开启「自动翻译划词」。
- 中文文献会被自动判定并跳过翻译；中文标题可以直接回填入库。

### 3.5 批注

- 高亮、写字、跳转批注，并支持导出带批注的 PDF。

### 3.6 文档问答与速读概览

- 右侧助手侧栏有四个面板：**问答 / 翻译 / 信息 / 摘录**。
- 问答使用本地 RAG 检索到的证据切片回答（见[第 9 节](#9-本地-rag-知识库)），回答中会给出文献与页码定位。
- **摘录面板**：面板内有两个标签页——「定位」（当前文献的所有摘录锚点）与「笔记」（当前文献关联的笔记，可按 全部 / PDF / AI / 笔记 筛选）。面板顶部可直接「加入当前笔记」或「新建笔记」；每条摘录与笔记都支持点击定位、复制标题/正文、删除。
- **速读概览**：为文献生成背景、研究问题、方法、实验设置、主要发现、结论、局限等结构化摘要，支持批量生成。

---

## 4. 笔记：使用

### 4.1 笔记的数据模型（先读这一节）

理解三件事，后面的操作都会顺：

1. **唯一事实源是 Tiptap JSON**。每篇笔记在库里保存了四份表示：`contentJson`（编辑器文档，事实源）、`contentHtml`（渲染缓存）、`contentText`（用于搜索的纯文本）、`content`（原始文本）。Markdown 只是导出与交换格式，不是存储格式。
2. **锚点是证据，不是装饰**。锚点（`anchors[]`）指向文献的具体位置：`paperId`、`pageIndex`、`blockId`、`pdfLocation`、摘录原文 `excerpt`、页码标签 `label`。它由系统在划词/提炼时写入，界面不提供直接编辑入口，Agent 也只能新增、不能改指或伪造。
3. **分类（文件夹）与笔记在同一个数据库里**。笔记分类不再是浏览器本地设置，而是入库数据，会随 WebDAV 备份一起走，MCP 与 Agent 也都能看到并操作。

一篇笔记的字段包括：标题、类型（`highlight` 划词摘录 / `area` 区域摘录 / `standalone` 独立笔记 / `ai-chat` 问答存档）、正文四份表示、标签、颜色、所属分类、摘录原文、PDF 位置、锚点、关联文献、双链、反向链接、字数、置顶、收藏，以及软删除标记。

### 4.2 笔记工作区界面

Notes 工作区是三栏布局：

- **左栏**
  - 顶部 `Notes` 与笔记总数；右侧三个按钮：**重建笔记语义索引**（配置 Embedding 后用于重建全部笔记的语义索引）、**同步 Markdown vault**（Obsidian 兼容）与**新建笔记**。
  - 搜索框：按标题、正文、摘录做关键词匹配（输入停顿约 0.2 秒后刷新）。
  - `Workspace` 分组下是 **全部笔记**、**未分类**，以及自定义分类树；分类支持折叠、右键菜单、新建子分类，**笔记可以直接拖到分类里**。每个分类与笔记都显示条目数。
- **中栏**：编辑器。左上角「图谱」按钮可切到笔记图谱与体检视图；正文底部显示字数、保存状态和保存按钮。
- **右栏**（可用手柄折叠/展开）：
  - **Outline**：按标题层级实时生成大纲。
  - **参考文献**：从笔记内容实时派生的引用列表，编号与正文中的 `[n]` 同源，每条带「第 N 页 / 定位」芯片，点击跳回原文。
  - **Backlinks**：反向链接——哪些笔记用 `[[...]]` 引用了当前笔记。

### 4.3 创建笔记的四种入口

| 入口 | 位置 | 产生什么 |
| --- | --- | --- |
| 新建空白笔记 | Notes 工作区左上角 `+`，或分类右键「在此分类新建笔记」 | 落到当前分类的笔记 |
| 划词「加入笔记」 | 阅读器划词浮层 | 把选中原文作为摘录插入**当前笔记**（没有笔记时自动新建一篇），保存后定位同步 |
| 划词「AI 提炼为摘录卡」 | 阅读器划词浮层（需配置 AI） | 两步 CoT 提炼成一张**摘录卡**：锚点块 + 提炼正文 + 「我的想法」区，自动打标签 `摘录卡` |
| Agent / MCP 写入 | Agent 审批卡 / 外部 Agent 经 MCP | 由 Agent 创建的笔记（详见[第 5 节](#5-笔记维护)） |

从模板起头也是常用方式：编辑器工具栏的 `Template` 菜单或斜杠命令 `/` 里可直接插入模板（共 11 个，见 [5.2](#52-页面类型与模板)）。

### 4.4 编辑与排版

**工具栏**（从左到右）：撤销/重做、正文与一级/二级标题、加粗/斜体/删除线/高亮、无序/有序/任务列表、引用、代码块、组件块、`Template`（模板菜单）、`引用`（文献引用菜单）、表格、公式、链接、图片、AI 润色。

**斜杠命令**：在空行输入 `/` 唤出菜单，可插入段落、各级标题、列表、任务列表、引用、代码块、表格、组件块、行内数学公式、文献引用、参考文献列表，以及全部模板。

**块级右键菜单**（在正文块上右键）：复制选中文本/整块、剪切、粘贴、加粗/斜体/删除线/高亮、清除文字样式、设为正文或标题、转换为无序/有序/任务列表、引用、代码块、在下方插入一行、在下方插入组件块、复制并插入当前块、删除当前块。表格还有独立子菜单：增删行列、切换表头行列、合并/拆分单元格、删除表格。

**公式**：支持行内与块级 LaTeX（KaTeX 渲染），从 LaTeX 直接粘贴即可。

**图片**：可以填图片 URL 或本地 data URL，也可以直接把图片文件粘贴或拖进编辑器。

### 4.5 组织笔记：分类、标签、双链、搜索

- **分类（文件夹）**：见 4.2。删除分类会级联删除子分类，其中的笔记**移动回未分类**，不会被删除。
- **标签**：标题右侧的标签按钮打开标签面板，逗号分隔输入；下方「常用标签」可一键添加。正文里写的 `#标签` 会在保存时被提取为笔记标签。MCP / Agent 写入时自动去掉 `#` 前缀，单篇上限 30 个。
- **双链**：输入 `[[` 唤起笔记补全，插入 `[[笔记标题]]`（目标需已存在）。链接写入时记录目标笔记 ID，目标改名后依旧能跳转；只有手工写在 vault / Markdown 侧的纯标题双链按当前标题匹配，改名后可能失效。
- **搜索**：左栏搜索框做关键词匹配；点击笔记中的 `#标签` 可以把列表过滤到该标签，右键菜单里有「清除筛选」恢复。
- **置顶与收藏**：笔记右键菜单里切换；列表默认置顶优先、更新时间倒序，顶部可切换按创建时间 / 标题排序。
- **批量整理**：选中多篇笔记后可批量追加同一标签，或一次性移动到某个分类。
- **复制**：右键可复制标题或复制正文，便于粘到别处。

### 4.6 关联文献与位置锚点

- **内联文献引用**：输入 `@` 或使用工具栏 `引用` 菜单搜索文献插入；正文中渲染为引用标记。点击引用：能定位到页/块时直接跳到原文，否则打开该文献。
- **摘录锚点块**：划词成卡或提炼时生成，块内显示原文摘录和页码标签（如 `P20`），点击「点击定位到原文」滚回 PDF 对应页/块。
- **右侧参考文献面板**：每篇被引文献显示作者、年份与最多 4 个引用位置芯片，点击任意芯片跳转。
- **多锚点**：一条笔记可以挂多个来源锚点（跨页、跨段累加），这是摘录卡的正常用法。

### 4.7 学术引用与参考文献列表

- **内联编号**：同一篇文献在全文共享同一个编号，编号按「首次出现顺序」分配，由系统从文档实时派生，**不写进文档数据**。因此中间插入一条新引用时，全文编号会自动重排；切换样式也不会污染正文。
- **引用样式**：工具栏 `引用` 菜单里可切换 **GB/T 7714-2015（默认）/ APA 7 / IEEE**。样式只影响渲染与文献列表文本：GB/T 与 IEEE 内联显示 `[n]`，APA 内联显示 `(第一作者 et al., 年份)`。
- **文末参考文献列表**：点 `引用 → 插入/更新文末参考文献列表`，按当前样式把列表写入正文（重复点击会覆盖更新，不重复追加）。右侧面板的列表不需要写入，始终实时显示。
- **数据来源**：优先用库内结构化字段（作者、年份、期刊/会议、卷、期、页码、DOI），缺失时退化为 `citation` 预设串，再退化为标题。文献被移出库后，列表项仍保留标题兜底。
- **导出约定**：Markdown 导出（vault）会把内联引用写成 `[n]`，并在文末附 GB/T 7714 参考文献列表，编号与内联一致。

### 4.8 摘录卡：划词 → 提炼 → 我的想法

摘录卡的设计前提是：**你读论文时多数不是逐字摘抄，而是提炼几句、改写一下、再写点自己的想法。**

一张摘录卡的结构：

```text
[锚点块] 📎 来源标签 + 原文摘录（保真，不手改）
[提炼正文] 你的浓缩与改写（可自由编辑，AI 可以参与）
[💭 我的想法：] 你的独立观点（不在原文中的推断写在这里）
```

- **生成**：PDF 或结构块里划词 → 浮层点「AI 提炼为摘录卡」。提炼分两步（先清洗识别质量、再改写提炼），完成后先打开左右对照的预览面板——左侧原始识别文本、右侧可编辑的标题与提炼正文，点「确认并写入」才落库；面板里也可用视觉模型重识别公式 / 表格区域，失败自动回退纯文本。写入后标题自动生成，标签自动加 `摘录卡`，锚点指向当前选区。
- **多段累加**：换页、换段继续划词，浮层里会多出「追加到当前摘录卡」按钮——新锚点与提炼正文会插到「我的想法」之前，已有锚点与原文快照**只增不改**。这就是「一条提炼挂多个来源」的操作方式。
- **原文快照**：默认只保留锚点（随时可以跳回 PDF 看原文，PDF 才是终极事实源）。需要逐字留存时，用「摘录卡」模板里的「原文快照」区；**写入后不要再改它**。
- **手动路径**：只想快速记一条原文摘录时，用「加入笔记」把摘录插入当前笔记，再自己补想法。
- **信任边界**：提炼正文随便改（AI 提炼、后续整理、Agent 审批后修改都允许）；锚点与原文快照必须保真。这是整个笔记体系的信任基础。

### 4.9 AI 润色

- 工具栏 AI 按钮打开润色面板，**润色范围**三选一：`仅优化文字与排版` / `笔记关联文献`（默认）/ `整个知识库`。
- 选择后两档时，应用会先在你的本地 RAG 知识库里检索证据，再把「可跳转的文献引用」一并写进结果；面板会提示「已附 N 条可跳转文献引用」。
- 结果先预览，可以「重新生成」或采纳；采纳后正文更新，引用节点会自动进入编号与文献列表体系。
- 依赖项：AI 模型配置（必需）；`笔记关联文献`/`整个知识库` 还需要 Embedding 配置与相应文献已建立 RAG 索引。没有关联文献或没有索引时，面板会给出明确提示并退回纯文字润色。

### 4.10 笔记图谱

点编辑器左上角的「图谱」按钮切换到图谱视图：

- 布局按**分类（或论文归属）分组**：同组笔记聚在一起，跨笔记的 `[[双链]]` 以虚线相连。
- 节点大小反映字数，颜色反映笔记类型（划词摘录、区域摘录、独立笔记、问答存档）。
- 点击节点打开对应笔记。

### 4.11 笔记体检

图谱视图顶部有一排体检徽标，点击可高亮同类问题的笔记节点，再次点击取消：

| 徽标 | 判定标准 | 处理建议 |
| --- | --- | --- |
| 孤立 | 无标签、无双链、无论文关联，且未归入任何分类 | 补标签、移动分类，或合并进概念页/索引页 |
| 断链 | 双链指向了已不存在（或已删除）的笔记 | 改指向新标题，或删除该链接 |
| 无标题 | 标题为空或仍是「未命名笔记」 | 补一个能作为双链锚点的标题 |
| 无标签 | 没有任何标签 | 补 1–3 个主题标签 |
| 陈旧 | 超过 30 天未更新 | 复核结论是否仍成立，或归档到索引/日志页 |

体检视图还提供「让 Agent 修复」入口：把当前问题整理成修复指令并打开 Agent（删除类只列清单，断链优先按已有笔记 ID 重挂，锚点与原文快照保持保真）。

体检是「按需自查」的工具，不是必须清零的指标：陈旧不等于无效，摘录卡也不必强求标签。

### 4.12 保存与外部更新

- **保存**：正文、标题、标签的改动会显示为未保存状态，点编辑器底部的 `Save` 或按 **Ctrl / Cmd + S** 写入数据库。未保存的内容在应用内切换笔记时以草稿形式保留，但**退出应用会丢失**，离开前请保存。
- **保存时会自动重算**：纯文本、字数、双链标题、文献引用、锚点集合（只保留仍然出现在正文里的锚点）、分类与标签。
- **外部更新提示**：当同一篇笔记在别处被保存（阅读器侧栏、Agent、MCP）时，编辑器会显示「有外部更新」。若你的编辑器里还有未保存内容，点「刷新」会用外部版本覆盖当前编辑内容——先复制草稿再刷新。
- **同一张卡在多个位置编辑**：阅读器侧栏的「摘录」面板与 Notes 工作区操作的是同一份数据，不要同时编辑同一篇笔记。

---

## 5. 笔记：维护

> 本节是手册的重点。维护的目标不是「写更多笔记」，而是让笔记能长期被自己（和 Agent）找到、复用、追到原文。规则定义见仓库文档 [笔记宪章](./notes-charter.md)，本节给出可直接执行的操作口径。

### 5.1 维护模型：三层架构与三条红线

```text
第 1 层  原始来源（不可变）—— 文献库 PDF、MinerU 结构块、划词高亮
第 2 层  笔记 Wiki（持续维护）—— 摘录卡、论文卡片、概念页、综述页、问答页、索引/日志
第 3 层  Schema（规则）—— 页面类型、命名与链接约定、标签词表（= 笔记宪章）
```

**三条红线**（跨应用、跨 Agent 一致）：

1. **Tiptap JSON 是唯一事实源**：Markdown / 纯文本是交换格式。用外部工具重写笔记正文会重建文档，正文中的结构化内容（锚点块、文献引用节点、表格等）会退化。
2. **锚点 ID 不可丢**：编辑、AI 改写、Markdown 往返都不能改变锚点指向；不能伪造锚点。
3. **提炼可以自由，证据必须保真**：提炼正文随便改；来源锚点与原文快照一个字都不能动。

### 5.2 页面类型与模板

每篇笔记应该属于一种页面类型；类型决定模板与最低内容要求，不决定文件夹归属。

| 类型 | 用途 | 最低要求 | 对应模板 |
| --- | --- | --- | --- |
| 论文卡片 `paper-card` | 一篇文献的结构化档案 | 建议挂 paperId；必须含「我的判断」 | 论文卡片 |
| 概念页 `concept` | 一个概念/方法/术语的定义与理解 | 必须有定义段；尽量挂来源锚点 | 概念页 |
| 综述页 `synthesis` | 跨多篇文献的比较、归纳、研究脉络 | 至少引用 2 篇文献 | 综述页 |
| 问答页 `qa` | 一个具体问题及其回答与证据 | 必须有「证据位置」段 | 问答整理 |
| 摘录卡 `excerpt` | 对一段原文的提炼式摘录 | 必须有来源锚点 | 摘录卡 |
| 索引页 `index` | 一个主题/项目的入口与目录 | 主要由 `[[双链]]` 组成，少写正文 | 索引页 |
| 日志页 `log` | 按时间推进的研究日志 | 条目倒序，每条带日期 | 研究日志 |
| 总览页 `overview` | 全局学习/研究状态 | 每周刷新一次 | 总览页 |

编辑器另有四个通用写作模板：文献阅读、方法拆解、实验记录、问答整理。模板可从工具栏 `Template` 菜单或 `/` 斜杠命令插入，插入位置是当前光标处。

**推荐的最小维护闭环**：划词摘录 → 每周把散摘录按主题聚合成概念页 → 每月给主攻方向写一页综述页/总览页，用 `[[双链]]` 串起来。

### 5.3 命名、链接与标签约定

- **双链按 ID 解析**：`[[标题]]` 写入时记录目标笔记 ID，目标改名后旧链接依然有效；Markdown 侧手写的纯标题双链没有 ID，仍按当前标题匹配，改名后可能失效。
- **概念页用术语命名**：「KV Cache」「FlashAttention」，不加「笔记」「总结」后缀。
- **论文卡片用「年份-第一作者-短题」**：如「2023 Vaswani Attention Is All You Need」。
- **索引页链接而非复制**：只放 `[[双链]]` 和一句话说明，不粘贴正文。
- **分类是归属，标签是横切**：一篇笔记只在一个分类；项目/领域用分类，主题/方法用标签。建议标签词表保持克制（每个方向 5–10 个）。

### 5.4 用内置 Agent 维护笔记

Agent 工作区可以直接读写笔记（需在本地文库模式下）：

| 工具 | 类型 | 说明 |
| --- | --- | --- |
| `search_notes` | 只读 | 按关键词与语义（配置 Embedding 时为向量 + 全文混合）检索，支持文献、标签、页面类型过滤，返回摘要与命中通道 |
| `read_note` | 只读 | 读出某篇笔记的标题、正文、标签、锚点与关联文献/笔记 |
| `write_notes` | 写（审批） | 批量创建/更新/删除，生成**笔记变更计划卡**，逐条列出操作、原因与正文预览，点「确认写入」才落库 |

可以直接对 Agent 说：

- 「把我库里关于 X 的散摘录整理成一页概念页，用双链串起来。」
- 「检查所有没有标签的笔记，给它们各补 2 个标签，先给我看计划。」
- 「把这次调研结论存成一篇笔记，挂在《XXX》下面。」
- 「找出双链断掉的笔记，列出修复计划。」

注意几点：

- Agent 创建的笔记正文是纯文本，编辑器会重建富文本；新建笔记默认落在未分类（Agent 不指定分类）。
- 更新操作会把新正文替换原正文并清空富文本结构；对**摘录卡**这类含锚点块和引用节点的笔记，不要让 Agent 整篇重写，最多追加或改标签。
- 删除是软删除（可恢复），但计划卡上仍会逐条列出，请核对清单再确认。
- 一批操作部分失败时不会整体回滚，计划卡会给出成功/失败计数与错误明细。

### 5.5 用外部 Agent（MCP）维护笔记

PaperQuay 内置标准的 MCP stdio 服务（`bin/paperquay-mcp.cjs`），Codex、Pi、Proma、Claude 等外部 Agent 无需打开桌面应用即可连接本地笔记库。配置方式见 [MCP 接入指南](./MCP_AGENT_INTEGRATION.md)，本仓库还提供了 `skills/paperquay-notes/SKILL.md` 作为外部 Agent 的作业规范。

笔记相关工具：

| 工具 | 说明 |
| --- | --- |
| `search_notes` | 混合检索笔记：配置 Embedding 时向量 + 全文，未配置时关键词（只读） |
| `list_note_tags` / `list_note_folders` | 标签与分类树（只读） |
| `create_note` | 新建：标题、正文（Markdown/纯文本）、标签、`type`、`paperId`、`folderId` |
| `update_note` | 局部更新：标题、正文、标签、`folderId`（传空串移动到未分类） |
| `delete_note` | 软删除 |
| `create_note_folder` / `rename_note_folder` / `delete_note_folder` | 分类树维护（删除分类时笔记移动到未分类） |

写操作护栏（与文库写工具同一套）：

1. `PAPERQUAY_MCP_WRITE=off` 时全局只读，优先级最高；
2. 检测到 PaperQuay 桌面应用在运行则**显式拒绝**写入，提示先关闭应用；
3. 确需并行写入时可传 `allowWhileAppRunning: true`；写入后需要重新加载应用界面才能看到（界面持有内存快照）；
4. 已知盲区：进程检测只能识别打包后的 `PaperQuay.exe`，开发模式（`electron .`）下护栏不生效。

使用边界：

- 正文用 Markdown/纯文本；`[[笔记标题]]` 会被解析为双链（目标笔记需已存在），`#标签` 自动归一化并与界面一致地写入标签表。
- 替换 `content` 会清空富文本缓存，编辑器按纯文本重建——**不要用 MCP 覆盖含锚点块、文献引用或表格的笔记**；这类笔记请在应用内编辑，或只用 MCP 改标题/标签/分类。
- 批量删除前先让 Agent 列出待删清单并人工确认。

### 5.6 Markdown vault 与 Obsidian 双向同步

如果你希望在 Obsidian 里查看或批量编辑笔记，可以把笔记库同步成一个本地 Markdown vault。

**开启同步**：Notes 工作区左上角的「同步 Markdown vault」按钮 → 首次点击会要求选择目录（可以直接指向一个 Obsidian 库）→ 之后每次点击执行一次同步，并在侧栏报告「导入 / 导出 / 清理 / 冲突副本」统计。已配置 vault 时，打开笔记工作区还会自动同步一次；设置 → 通用 →「笔记与 Vault」里可开启 15–60 分钟定时同步（默认关闭）。

**目录与文件**：

- 每篇笔记一个 `.md` 文件，分类即目录层级，文件名是笔记标题；标题或分类变化会改名/移动，旧文件按清单自动清理。
- 同名文件冲突时，新文件会带 `--<id 后 6 位>` 后缀，不会覆盖你的文件。
- 同步清单写在 vault 根目录的 `.paperquay-vault.json`：**只有清单记录过的文件会被本应用删除或改写**，你自己放进 vault 的其他 Markdown 不会被碰。

**frontmatter 字段**：`id`（同步锚，禁止在 Obsidian 侧修改）、`type`、`paperId`、`folder`、`tags`、`sources`（关联文献）、`createdAt`、`updatedAt`、`anchors`（锚点数据，导出时原样写入）。

**导入 / 导出规则**：

| 情况 | 行为 |
| --- | --- |
| 无 `id` 的新文件 | 视为新笔记导入；所在目录会自动映射成分类（不存在则创建） |
| 有 `id`，文件侧有变化且库内未变（以 frontmatter `updatedAt` 为主时钟，缺失时回退修改时间） | 用文件内容更新库内笔记的标题、正文、标签、分类 |
| 有 `id`，库内与文件在**上次导出后都发生了变化** | 不覆盖原文件：应用侧版本保留，文件内容另存为 `--conflict-YYYYMMDD-HHmmss.md` 冲突副本，供人工合并 |
| 有 `id`，但笔记已在应用内删除 | 跳过，不复活（文件留给你自行处理） |
| 库内笔记 | 导出为 Markdown：内联引用写成 `[n]`，锚点块写成 `paperquay://anchor/<id>` 链接，`[[双链]]`、`#标签` 原样保留，文末附 GB/T 7714 参考文献列表 |

**需要注意**：

- 导入回来的正文是纯文本 → 原来的富文本结构（锚点块、引用节点、表格等）会退化。**不要把 vault 当成富文本编辑的替代路径**：批量整理文字、重命名、调结构适合在 Obsidian 做；带锚点的摘录卡、需要跳转定位的引用，回 PaperQuay 编辑。
- frontmatter 里的 `anchors` 只是导出记录，导回时不会被写回数据库（库内锚点不受影响）。
- 推荐姿势：Obsidian 负责「读、搜、批量改文字」，PaperQuay 负责「锚点回跳、编号引用、图谱与体检」。

### 5.7 备份与迁移

- **WebDAV 备份**：设置 → 备份 / WebDAV，配置地址与账号后手动执行。上传内容包括三个数据库（文献库、笔记库、RAG 库）；**笔记分类、标签、双链、锚点都在笔记库里**，所以备份即完整。可同时备份 PDF 与派生文件。
- **恢复**：同一面板选择远端备份恢复；恢复以备份包中的数据库为准。
- **迁移/换机**：直接复制整个 `<数据目录>/PaperQuay` 目录与文献存储目录即可；vault 目录是普通文件夹，单独拷贝即可作为只读归档。
- **版本兼容**：数据库结构随版本演进，建议「先备份、再升级」，不要让新旧版本交替写同一个目录。

### 5.8 维护节奏建议

| 频率 | 动作 |
| --- | --- |
| 每次阅读 | 划词提炼或加入笔记；补 1–3 个标签；有想法就写进「我的想法」区 |
| 每篇文献读完 | 用「论文卡片」模板写一页：研究问题、方法、实验、我的判断 |
| 每周 | 打开图谱，看一遍体检徽标；把散摘录聚合成概念页；修正断链；执行一次 vault 同步做文本层整理 |
| 每月 | 刷新总览页；检查陈旧笔记是否仍成立；执行一次 WebDAV 备份 |
| 换机/重装前 | 备份 + 记录文献存储目录与 vault 目录路径 |

### 5.9 笔记常见问题

| 现象 | 原因与处理 |
| --- | --- |
| 点引用没跳到对应页 | 该引用没有携带位置信息（只有 paperId）——它只会打开文献；带位置的引用来自划词/润色/摘录锚点 |
| 保存后锚点按钮消失 | 保存时只保留仍出现在正文里的锚点；删除锚点块后锚点会一并清理，需要重新摘录 |
| `[[双链]]` 点击无反应 | 目标笔记不存在；编辑器内插入的双链带目标 ID、改名不受影响，手写在 Markdown 侧的纯标题双链需在目标改名后按当前标题更新 |
| 笔记列表里看不到旧笔记 | 工作区一次加载最多 1000 条（置顶优先、更新时间倒序）；用搜索、标签或分类定位较早的笔记 |
| 编辑器和阅读器侧栏显示的内容不一致 | 同一个 noteId 在两个位置被编辑；以最后保存的为准，用编辑器「刷新」加载外部版本 |
| vault 同步后 Obsidian 里出现重复文件 | 同名冲突或双侧修改冲突会产生 `--xxxxxx` / `--conflict-YYYYMMDD-HHmmss.md` 副本；确认内容后再手工合并，不要直接删应用的清单文件 |
| 想批量改标题/标签 | 用 Agent（内置或 MCP）走计划/预检；不要用脚本直接改数据库 |

---

## 6. Agent 工作区

- **对话式文献操作**：Agent 可以直接调用工具完成文献检索、详情读取、RAG 检索、图片读取、记忆读写；写操作（重命名、元数据、分类、标签、笔记）一律先产出**审批计划卡**，确认后落库。
- **执行轨迹**：每一步工具调用、参数与结果都在轨迹里可见，便于核对。
- **多阶段调研流水线**：复杂问题（对比调研、主题综述）会自动拆成检索—阅读—归纳的多阶段流程，受上下文预算控制，分批加载文献正文。
- **三层记忆**：`agent-memory/` 下保存运行轨迹、L2 摘要与 L3 长期记忆；记忆写入同样走审批卡。
- **思考强度与用量**：每次运行可选择思考强度，运行记录包含 token 用量，便于控制成本。
- **会话管理**：多会话、切换、删除、中断恢复；切换工作区不会中断正在运行的会话。

## 7. 知识图谱

- 节点覆盖文献、笔记、标签、分类与引用关系；边包括语义相似、Crossref 参考文献同步、共同作者、自定义与 AI 推断关系。
- 全局使用 fcose 力导向布局；选中节点可查看局部同心圆视图；支持导出 PNG/JSON。
- 笔记的双链与文献关联会进入图谱，适合观察一个方向的文献—笔记结构。

## 8. 综述写作

- 流程：检索上下文 → 生成写作蓝图（章节、段落数、核心论点、图表插入点）→ 分段并发写作 → 失败任务可独立续跑 → 选择导出位置 → 导出 Word。
- Word 导出支持 OMML 公式、中英文标题、参考文献与正文插图。
- 蓝图与段落任务都可手工调整，写作过程中的引用来自本地 RAG。

## 9. 本地 RAG 知识库

- **启用**：设置 → `概览与问答` 中打开「启用本地 RAG」，并在 `AI 模型` 配置 Embedding（端点、模型、维度）。
- **检索来源模式**：决定索引从 MinerU Markdown、PDF 文本还是两者混合提取；`Top-K 检索块数` 控制每次送入模型的切片数（建议 4–8）。
- **索引管理**：设置里有「知识库索引管理」控制台——显示已索引 / 待索引 / 失败统计，支持批量建立或强制重建、进度条、暂停/继续/取消；文献列表有实时状态徽标（已索引 / 索引中 / 未索引 / 失败），右键可对单篇断点续传重试。
- **检索质量**：配置 Embedding 后是「向量 KNN + FTS5 全文 + RRF 融合」的混合检索，未配置时退化为关键词检索；证据卡片支持「查看切片上下文」，可浏览前后邻居切片与章节路径。
- **谁在用 RAG**：文档问答、Agent、笔记 AI 润色（关联文献/整个知识库两档）、综述写作。

## 10. MCP 与外部 Agent 接入

- 服务入口：`bin/paperquay-mcp.cjs`（stdio），直连本地 SQLite，不需要桌面应用保持运行。
- 能力：文献检索与详情、知识库混合检索与正文切片、Zotero 浏览/检索/差量预检/选择性同步、文库写入（导入 PDF、分类管理、元数据、删除）、**笔记维护**（检索、创建、更新、软删除、标签与分类树）。
- 安全边界：所有写工具默认在检测到桌面应用运行时拒绝写入，`allowWhileAppRunning` 可覆盖，`PAPERQUAY_MCP_WRITE=off` 全局只读。
- 完整工具清单、客户端配置示例（Proma / Pi / Codex / Claude）与自定义数据目录说明见 [MCP 接入指南](./MCP_AGENT_INTEGRATION.md)。

## 11. 数据、备份与隐私

- **本地优先**：文献库、笔记库、RAG 索引都在本机 SQLite 中；PDF 保存在你指定的存储目录。
- **会离开本机的数据**：调用外部服务时才会发送，包括——翻译/概览/问答/Agent（你配置的模型接口）、MinerU 或 PaddleOCR-VL 解析（PDF 内容）、OpenAlex/Crossref 元数据查询（标识符）、Embedding 检索与索引（查询文本或切片文本）。配置界面里可以看清每条链路使用哪个端点。
- **密钥**：API Key 存在本地设置文件中，不会随文库数据外传；导出/分享数据目录前请自行清理。
- **不要纳入同步或仓库的内容**：数据库文件、PDF、解析产物、备份包、密钥文件（仓库 `.gitignore` 已覆盖这些类别）。

## 12. 设置参考

| 分区 | 关键项 |
| --- | --- |
| 通用 | 界面语言、主题（浅色/深色）、基础行为、笔记与 Vault（笔记引用样式、vault 定时自动同步） |
| 文库与 Zotero | 文献存储目录、导入模式、命名规则、是否创建分类文件夹、OpenAlex Key/mailto、Zotero 数据目录与导入 |
| 阅读显示 | PDF 与结构块联动、滚动、布局、结构块显示选项 |
| 文档解析 | 识别引擎（MinerU / PaddleOCR-VL）、API Key、解析缓存目录、自动解析、批量任务、图片引用修复 |
| 备份 / WebDAV | 远端地址、账号、备份范围、连接测试、立即备份/恢复 |
| 翻译 | 全文与划词翻译开关、语言、并发与吞吐、翻译缓存清理 |
| AI 模型 | 模型预设、功能与角色绑定、Embedding 配置 |
| 概览与问答 | 概览输入来源、批量概览、本地 RAG 开关、检索来源模式、Top-K、索引管理 |
| 软件更新 | 检查 GitHub 发布版本并安装更新（macOS 跳转发布页手动下载） |

## 13. 更新与排障

- **更新**：设置 → 软件更新；Windows 与 Linux 支持应用内自动更新，macOS 打开发布页手动下载安装包。升级前建议先做一次 WebDAV 备份。
- **入口排障顺序**：解析异常 → 看文档解析状态与缓存；检索/引用异常 → 看 Embedding 与索引统计；翻译/概览异常 → 看 AI 模型预设与连通性测试；文库写入失败（MCP）→ 确认桌面应用已关闭。
- **性能**：超长文档首屏会分段渐进可用；大文库列表用 SQL 分页与行窗口化，若仍卡顿，优先检查是否有超长解析文档正在后台解析。

---

## 附录 A：快捷键

| 快捷键 | 作用 |
| --- | --- |
| `Ctrl / Cmd + S` | 保存当前笔记 |
| `Esc` | 关闭划词浮层 / 设置窗口 |
| `Delete` / `Backspace` | 删除 PDF 中选中的批注（批注模式下） |

## 附录 B：常见问题速查

| 问题 | 一句话答案 |
| --- | --- |
| 笔记会随 WebDAV 备份吗？ | 会，笔记库包含正文、分类、标签、锚点与双链 |
| 可以用 Obsidian 编辑笔记吗？ | 可以，走 Markdown vault 双向同步；但带锚点块/引用节点的笔记不要在 Obsidian 里重写 |
| Agent 改笔记会直接生效吗？ | 不会，会先出「笔记变更计划」卡，确认后才写入 |
| 外部 Agent 写入被拒绝了？ | 关闭 PaperQuay 桌面应用后重试，或显式传 `allowWhileAppRunning: true` |
| 引用编号需要手工维护吗？ | 不需要，编号从文档实时派生，插入/删除引用会自动重排 |
| 摘录卡的原文会被 AI 改掉吗？ | 不会，锚点与原文快照保真；可自由改写的是提炼正文 |
| 换电脑怎么迁移？ | 复制 `<数据目录>/PaperQuay` 与文献存储目录（vault 目录可一并拷贝） |
