# 更新日志

本文档记录 PaperQuay 各版本的主要变更。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

各平台安装包见 [GitHub Releases](https://github.com/Dongyurocket/PaperQuay/releases)。

## [0.1.38] - 2026-09-08

### 新增

- **不满意区块 AI 大模型重新识别与重构**：在 BlockViewer 结构块悬浮操作栏（「✨ AI 重析」）和右键菜单中支持对任意识别不满意的区块调用大模型进行二次重构与排版修复；支持下拉自由选择用户配置的任意大模型，并提供「智能排版纠错」、「表格/术语表结构化」、「数学公式提取」三种模式及补充指令输入。
- **无多余内容严格约束与双重防御剥离**：设计了极端严密的负向约束系统提示词（强制禁止任何问候、开场白、解释废话或代码块包裹），并在 Node.js 后端与渲染层配备双层防御性清洗机制，自动剔除思考标签（`<think>...</think>`）、外层代码块定界符与可能逃逸的前置/后置废话，确保输出 100% 纯净学术 Markdown。
- **重析结果实时对比与覆盖还原**：在交互式弹窗中提供 KaTeX 渲染预览与 Markdown 源码对比，确认后一键应用到当前区块，并在区块头部标示「AI 已修复」徽标；支持一键撤销并恢复原始 MinerU 识别。

### 修复

- **首字下沉（Drop Cap）伪上标与断裂自动修复**：针对学术论文常见的首字下沉排版（如段首大号 `U` 跨两行高度导致右侧文本被判定为偏高上标 `U<sup>RBAN ...</sup>` 或 `U^{RBAN ...}`），自动规约并还原为标准大小写词汇（如 `Urban`），同时修复单字母与大写词干之间空格截断缺陷，消除异常留白与错位换行。
- **术语表（Nomenclature）无框表格自动重构**：彻底解决学术论文无框术语表被版面分析误判为普通文本段落、因“去除软换行”而压平坍缩为整团乱码的问题；智能解耦变量符号与描述之间的字符粘连（如 `Bnumber` $\to$ `$B$` 与 `number`、`C_Bbattery` $\to$ `$C_B$` 与 `battery`、`mmass` $\to$ `$m$` 与 `mass`、`cchord` $\to$ `$c$` 与 `chord`），自动将其重构为排版优雅的两列 Markdown 变量定义表，数学符号自动以 KaTeX 矢量公式呈现。

## [0.1.37] - 2026-09-08

### 修复

- **交叉引用误判伪上标智能清洗**：解决 MinerU 及 OCR 版面模型在遇到紧随标点的交叉引用（如 `Table 8,`、`Fig. 2,`、`Eq. 3,`）时误触发文献引用先验而将正文编号打上 `<sup>` 的问题；全面覆盖 `Table`、`Figure`、`Equation`、`Section`、`Algorithm` 等科技文献交叉引用实体，自动将 `Table <sup>8</sup>` 或 Unicode 上标 `Table ⁸` 还原为标准正文编号 `Table 8`。
- **连续引用与标点空格自动修复**：支持串联交叉引用（如 `Table 8 and 9`、`Figure 2, 3, and 4`）的连续上标还原，并自动修复剥离上标后遗留的逗号与后续单词粘连缺失空格缺陷（如 `Table 8,while` 自动修正为 `Table 8, while`）。
- **全链路一致性保障**：清洗逻辑在 BlockViewer 渲染、全文检索、RAG 向量切片、AI 摘要与翻译管道中全局生效，保障学术论文阅读排版与检索质量。

## [0.1.36] - 2026-09-08

### 新增

- **PDF 原始区域切片（BBox Crop）回退机制**：BlockViewer 结构块（段落、公式、算法、表格等）在悬浮或激活时提供「原 PDF 切片」操作按钮，支持在识别排版与原版 PDF 高保真矢量切片之间一键自由切换，便于随时对照原版排版与细微常数。
- **公式解析失败原切片兜底**：当公式因 OCR 识别缺陷或语法错误导致 KaTeX 无法解析时，红色错误卡片中直接提供「查看原 PDF 切片」操作，并在下方内联展开高清原图，科研阅读与公式核对 100% 准确不中断。
- **右键上下文菜单集成**：在任意结构块上右键，菜单中新增「切换为原 PDF 切片 / 显示排版文本」快捷操作。
- **Retina 2x 高清离屏渲染与缓存**：基于 PDF.js 矢量画布实现 2.0x 高清离屏裁剪与轻量 LRU 内存缓存，小字号上下标清晰可见，且支持点击大图预览。

## [0.1.35] - 2026-09-08

### 修复

- **连字与伪上标自动清洗**：消除 MinerU 解析结果中因西文连字（`fi`、`fl`、`ff`、`ffi`、`ffl`）及标点误判产生的 `<sup>fi</sup>`、`<sup>fl</sup>`、`<sup>–</sup>`、`<sup>’</sup>` 等伪上标乱码，自动还原为完整英文单词与正规标点；在结构化展示、全文搜索、RAG 向量切片与 AI 翻译主链路统一生效，彻底避免词汇碎裂。
- **正规学术上标优雅渲染**：新增零依赖 `remarkSuperscriptPlugin` 插件，将 Markdown AST 中的 `<sup>` 与 `<sub>` 转换为标准的上标/下标语义节点，并配置自适应排版样式；彻底解决单位（如 $kg/m^2$）、引用标号（如 $^{[1-3]}$）、作者注记等无法渲染或直接暴露 HTML 原始标签的问题。
- **公式检测 HTML 标签保护**：行内公式扫描器增加对 HTML 标签的识别保护，杜绝因将 `<`、`>` 误判为数学操作符而将普通文本误包装为 LaTeX 公式（避免渲染出怪异的 `< sup > 2 < / sup >`）。
- **公式定界符防撞与多行排版**：修复相邻行内公式拼接时缺少分隔符导致粘连生成非法 `$$` 并引发 KaTeX 语法报错变红的问题；优化被识别为 `algorithm` 块的公式与代码注释按行分段排版，保持清晰的上下结构层次。

## [0.1.34] - 2026-09-08

### 新增

- AI 笔记润色：Tiptap 笔记编辑器支持润色选中文本或生成整篇笔记的结构化版本；结果先预览，用户确认后才替换或插入。
- 润色可选择仅优化文字、仅使用笔记关联文献或使用整个本地知识库。检索证据写入为可点击的文献锚点，可跳转到对应论文的页码或结构块。

### 修复

- 模型只能返回服务器分配的检索证据编号，无法伪造引用文献、页码或结构块位置；Embedding 未配置、索引缺失或检索失败时明确提示并降级为纯文本润色。
- 修复独立笔记工作区跳转 RAG 引用时遗漏结构块和页码定位字段的问题。

## [0.1.33] - 2026-09-07

### 新增

- 中文文献支持：中文文献免翻译——全文翻译在检测到正文以中文为主且目标语言为中文时自动跳过；标题翻译对中文标题直接采用原标题，不再占用翻译接口；语言检测排除日文假名，避免日文文献误入中文流程。
- 中文全文检索：本地 RAG 知识库的全文检索分词器从 unicode61 迁移到 trigram，中文关键词可按子串命中正文切片（旧版把整段中文当成单个词，中文检索完全失效）；旧版数据库启动时自动重建索引，环境不支持时安全回退；英文检索在 trigram 下退化为子串匹配，召回能力更高。
- 解析后自动入库：MinerU 解析完成（单篇/批量、新解析/缓存复用）后，自动将文献的 markdown 源后台纳入本地 RAG 索引，文献无需打开阅读器即可被知识库检索；未启用本地 RAG 或未配置 Embedding 服务时静默跳过。
- 中文文献元数据智能补全：Crossref/OpenAlex 对中文论文覆盖有限，新增 LLM 兑底提取——从文献首页文本抽取标题、作者、年份、期刊、DOI、摘要、关键词、卷号、期号、页码、ISSN 等字段，复用「论文概览」模型预设；导入对话框（手动点「自动补全」时）、批量「解析元数据」与单篇「解析元数据」对话框均在远程检索未命中时自动兑底，英文文献不会触发模型调用。

### 修复

- 批量标题翻译增加中文直填失败计数；LLM 元数据提取增加 60 秒超时与条目类型白名单校验；提取结果无任何有效字段时不再计入「已匹配」。

## [0.1.32] - 2026-09-06

### 新增

- 知识库 MCP stdio 服务：内置独立的标准 MCP stdio 知识库服务（`bin/paperquay-mcp.cjs`），提供给 Proma、Pi、Codex、Claude Code 等外部 Agent 免侵入直连调用。基于 `node:sqlite` 原生只读模式直连本地 SQLite 数据库（文献库、RAG 分块、笔记），无锁且无需桌面端持续运行即可查询；支持文献检索（`search_papers`）、单篇详情（`get_paper_details`）、RAG 知识库切片证据检索（`search_knowledge_base`，含页码与段落定位）、正文切片阅读（`read_paper_content`）与笔记搜索（`search_notes`）；在旧版数据库上具备动态列自适应与平滑向下兼容。
- 译文 PDF 统一文件夹管理：文库偏好设置新增「统一译文 PDF 存放文件夹」（默认存放在 `<storageDir>/translated-pdfs`，亦支持自定义外部独立路径）。附加 retainpdf 对照 PDF 时自动复制到统一目录集中管理；修改存储目录时自动平滑迁移已有译文，且安全策略保证不误删共享或外部文件，向前兼容旧版根目录文件。
- 学术元数据与常用引用扩展：文献数据模型扩展支持书籍（Book）、书籍章节（Book Section）、学位论文（Thesis / Dissertation）、研究报告（Report）、会议论文（Conference Paper）、预印本（Preprint）等常用学术类型；新增出版社、授予单位/高校、报告号、卷号、期号、页码、ISBN、ISSN 等标准引用字段；SQLite 数据库自动无损列迁移；详情面板支持类型选择与动态字段编辑及卡片展示；BibTeX 导出支持生成标准的 `@book`、`@techreport`、`@phdthesis`、`@incollection` 条目。

### 优化

- 安全写路径白名单自动将自定义译文 PDF 存储目录纳入允许范围，防止跨盘符写入被拦截。
- Zotero 导入自动继承并规范化原始条目类型（书籍、学位论文、报告等）。

## [0.1.31] - 2026-08-27

### 修复

- 修复应用启动卡顿数秒的问题：知识图谱在隐藏挂载时不再立即构建，改为切换到图谱工作区时才首次加载；图谱语义相似边不再全量读取 chunk 向量（实测 7550 条 × 4096 维阻塞主进程 6.4 秒），改为文档级平均向量缓存表 + 两两余弦，相似边计算降至毫秒级；旧库首次启动一次性回填缓存（约数秒，仅一次）。
- 修复语义相似边一直静默为空的隐藏 bug：node:sqlite 下 sqlite-vec 返回 float32 原始字节，旧解码逻辑永不成立导致相似边恒为空，现已正确解码为 Float32Array。
- 修复启动性能次要因素：`library_init` 不再每次启动全库 DELETE + 重插；MinerU 产物状态检查由每篇文献一次 IPC 改为单次批量 `paths_exist` 检查，并复用启动快照避免重复全量拉取。
- 修复 Agent 运行 token 统计：对比调研 Capability 的模型调用用量现在计入运行总量，且不再重复计数。

### 新增

- Agent 中断恢复交互改进：检测到中断运行后，恢复内容（含完整工具调用参数）回填到输入框，由你确认发送后继续，不再未经确认自动重跑；放弃恢复时中断运行明确标记为已取消。

### 优化

- Agent 后端加固：单轮消息限制 80 条 / 80 万字符，图片附件限制 4 张 / 8MB；后端 Agent 轮次支持取消；流式响应正确合并 token 用量；请求超时与手动取消信号正确组合。
- Agent 记忆 trace 日志按字节数（而非字符数）执行大小限制，多语言内容计量更准确。
- Agent 输入框工具栏中「整理 Agent 记忆」按钮改用 BrainCog 图标，与思考强度选择器的 Brain 图标区分开。

## [0.1.30] - 2026-08-27

### 新增

- Agent 升级为 ReAct 多轮工具调用循环：模型可自主多轮调用只读工具（文库搜索、单篇元数据、概览、RAG 检索、请求上下文、读图、读记忆）后综合回答；写操作仍生成计划走既有审批流程；单轮上限 8 轮防失控，工具报错可自我修正；设置页保留「旧版 Agent 模式」开关可临时切回。
- Agent 消息新增可折叠执行轨迹：逐轮展示工具调用、结果摘要与耗时，运行中实时显示当前轮次与正在执行的工具。
- 本地 RAG 混合检索：在向量检索之外新增 FTS5 关键词通道，两路结果按 RRF 融合，显著改善专有名词、型号、缩写等向量不敏感查询的命中；FTS5 不可用的环境自动降级为纯向量检索。
- Agent 长会话上下文压缩：接近模型上下文上限时自动在用户轮边界压缩历史为结构化进度摘要，已读论文、引用页码等产物轨迹跨压缩保留。
- Agent 三层记忆：运行事件自动写入本地 `agent-memory/trace` 日志（L1），主题事实（L2）与跨主题综合（L3）为人类可读可编辑的 Markdown；Agent 每轮自动携带 L2/L3 摘要，并可通过审批后的写记忆工具更新；设置页新增「Agent 记忆」管理区。
- Agent 运行可观测：每次运行的轮次、工具调用与 token 用量落库（agent_runs/agent_run_events）；输入框旁实时显示本次运行累计 token，会话列表显示各会话总用量。
- 会话恢复与分支：运行中断（关窗/崩溃）后重开会话可提示从最近完整轮次恢复；任意消息可「从此处分支」复制出新会话。
- 对比调研 Capability：对选中的多篇论文执行「改写问题 → 分解子题 → 逐题检索 → 综合成文」四阶段流水线，UI 显示阶段进度，可取消、可续跑。
- Agent 视觉上下文：RAG 命中论文图片/表格块时自动将图片（压缩至最长边 1568、单轮最多 4 张、共 8MB）随行人模型请求，引用区显示缩略图可放大；模型 preset 新增「支持视觉」开关，未开启时跳过并明确提示。

## [0.1.29] - 2026-08-26

### 修复

- 结构阅读器不再漏显 MinerU 图表：`chart` / `figure` 块统一按图片块渲染，本地截图正常加载，图注取自 `chart_caption`。
- 修复空图注表格把 `<table>` HTML 源码显示为文本的问题：图注提取改为严格字段提取，无图注时不再回退到完整表格内容，结构化表格仍正常渲染。
- 修复全文翻译输入被图片路径与表格 HTML 污染的问题：结构性资源字段不再进入文本提取与 Markdown 回退；无图注且无 OCR 正文的视觉块不再提交翻译。
- 历史翻译缓存中以 `images/` 资源路径开头的污染译文自动失效，下次全文翻译重新生成，无需手动清理缓存。

## [0.1.28] - 2026-08-26

### 新增

- 文库页支持多选文献：Ctrl/Cmd 点选、Shift 范围选择、行首复选框；多选时列表顶部出现批量工具栏，支持批量删除、移动分类、收藏，详情面板切换为批量摘要视图。
- 批量标题翻译：对多选文献按批量并发设置逐篇调用翻译模型，中文标题直接写入文献信息，默认跳过已有中文标题。
- 批量导出 Bib：多选文献可导出为合并的单个 .bib 文件，或每篇文献一个 .bib 文件；自动生成去重的 citation key，按期刊/会议/预印本启发式推断条目类型。
- 思考强度新增「最高（max）」档，阅读器侧栏问答的思考强度选择器在窄宽度下不再消失，菜单项显示当前档位。
- 设置页新增「测试 Embedding 连接」，成功显示向量维度与耗时，404 时明确提示该服务缺少 embeddings 端点。

### 修复

- 修复概览缓存在文库预览与阅读器之间不互通的问题：两处此前使用不同的缓存键算法，导致已生成的概览在打开文献时重复生成；现已统一缓存键并自动迁移旧缓存。
- Agent 问答在本地 RAG 检索失败时不再静默回退：与侧栏问答一致，回答下方会显示未命中提示与错误原因。

## [0.1.27] - 2026-08-26

### 新增

- 文献支持中英双语标题：可手工编辑或调用翻译模型一键生成中文标题，列表与详情页支持“中英 / 中文 / 原文”三种显示模式并持久化，中文标题可搜索。
- 本地文献可附加 retainpdf 翻译版 PDF，阅读器新增“PDF 对照”模式：左侧原版、右侧翻译版，按页码双向同步，可随时关闭联动；文献库和阅读器内均可附加、替换、移除。
- 翻译版 PDF 随 WebDAV 备份上传，跨设备恢复后自动修正存储路径。

## [0.1.26] - 2026-08-25

### 修复

- 修复知识图谱节点全部聚集在中心重叠的问题：布局此前在图谱页仍隐藏（容器尺寸为 0）时就已执行完毕，所有节点被压缩在极小区域且不再重算；现在布局会等图谱页可见后才执行，切换到图谱页时自动补算。
- 修复在"自定义关系"面板输入文字时图谱被销毁重建、画布变空白的问题。

### 变更

- 全局图谱布局引擎从内置 cose 更换为 fcose，参数经基准实验标定，密集图谱分布更均匀，大图布局速度提升约 20 倍。

## [0.1.25] - 2026-08-11

### 修复与增强

- 文献问答会话按论文持久化保存，重启后可恢复当前会话、历史对话和预设；切换标签页或退出时及时落盘，不再被空白"新对话"覆盖。
- 全文翻译结果在重启后可靠恢复，包括使用自定义 MinerU 缓存目录的场景；缓存改为串行原子写入，读写失败明确提示，不再静默丢失。
- WebDAV 备份可靠性显著提升：针对坚果云限制进行请求节流，支持 `Retry-After`、瞬时错误重试、流式上传、自适应超时、失败对象与服务端响应明细、进度反馈，以及异常后的临时快照清理。
- 综述 Word 导出支持可编辑 OMML 公式、中英文节标题、正确首行缩进、更完整的参考文献信息、缺图容错，并可把图片插入模型生成的相关正文位置。
- 综述生成任务不再因单项失败而中止队列；已完成章节保留，失败任务单独上报，全部排队任务处理后仍可继续生成。

## [0.1.24] - 2026-07-09

### 新增与增强

- 综述 Word 导出支持可编辑 OMML 公式、缺失图片容错、本地化节标题、480 twip 首行缩进、更完整参考文献信息，以及根据模型输出在正文中插图。
- 综述生成更稳健：单个写作任务失败不会中止整个队列，更多检索论文可参与深度 RAG 上下文。
- 知识图谱支持同步 Crossref 参考文献数据，只为文库内论文生成引用关系，并补充共同作者关系、边颜色图例、图谱导出、关系目标搜索和节点右键交互。
- MinerU 解析支持配置 API Base URL，便于使用本地部署的 MinerU；未配置时仍默认使用官方地址。
- 修改默认文献存储目录时，会迁移已有目录结构和附件路径到新位置。
- 阅读与笔记流程新增划词翻译高亮、本地文库导航栏拖动调整宽度、文献列表排序，并修复本地保存后误提示外部更新的问题。

## [0.1.23] - 2026-06-13

### 优化

- macOS 下使用系统左上角三色窗口按钮，并隐藏重复的自定义窗口控制按钮。
- 优化 PDF.js 生命周期清理，减少切换、关闭或重新打开 PDF 时的旧 worker / 渲染任务报错。
- Agent RAG 回答的引用标签支持点击跳转到对应论文的结构块或页面。
- 增加并明确 RAG Top-K 设置，用于控制发送给模型的最相近上下文块数量。
- 优化划词翻译和 PDF 段落翻译浮层，支持独立开关，并改进浮层位置以减少遮挡。
- 优化侧边栏问答输入、多会话问答和 Agent 交互体验。

## [0.1.22] - 2026-06-11

### 优化

- 优化 PDF 阅读体验，包括更轻的启动行为、论文阅读 tab 常驻，以及老旧 PDF 和解析段落交互的兼容处理。
- 划词翻译浮层和 PDF 段落译文浮层提供独立开关，并优化浮层位置以避让当前选区或点击段落。
- 优化 Agent 对话体验，包括流式回复滚动、计划操作按钮颜色，文献范围入口改为更直观的"选择文献"。
- 强化 Agent 的论文上下文能力，可根据论文 ID 加载 MinerU 解析内容或 PDF 文本，用于全文分析、对比和总结。
- 继续整理笔记和阅读器工作流，包括全局侧栏笔记行为，以及笔记锚点、划词和 PDF 翻译之间的交互。

## [0.1.21] - 2026-06-08

### 新增

- PDF 阅读模式下的段落译文浮层：点击 MinerU 解析后的段落时优先显示对应缓存译文，无缓存时可单独翻译该段。
- 段落译文和划词翻译浮层根据段落或选区的屏幕矩形避让，尽量不遮挡选中内容。
- 翻译工作流交互优化。

### 修复

- 修复 PDF 中加入笔记后的锚点高亮阻塞问题：保存到笔记的段落仍保留高亮提示，但不再影响后续划词、点击段落或触发翻译。
- PDF 左侧缩略图侧栏首次进入时默认收起，降低首屏加载压力。

## [0.1.8] - 2026-06-06

### 新增

- 文献详情面板新增阅读时间图，基于 PDF 阅读热力数据展示总阅读时长、活跃阅读区段和最后阅读时间。
- 文献列表阅读热力预览、阅读器热力进度和详情面板阅读时间图统一数据来源。

### 修复

- 修复发布说明中的中文乱码，中文 release notes 可正常阅读。

## [0.1.7] - 2026-06-06

### 变更（大规模重构）

- 项目围绕新的 Electron 桌面运行时、共享平台服务和更清晰的功能模块重新组织。
- 新增笔记工作区：文件夹组织、笔记元信息、标签、反向链接、大纲、右键菜单和更完整的编辑器控制。
- 笔记支持内联知识链接：wiki 风格笔记链接、论文引用、标签和笔记定位链接，可在相关内容之间快速跳转。
- 笔记与阅读器联动：笔记保留 PDF 来源定位，并可跳回对应阅读位置。
- 编辑体验升级：斜杠菜单、模板、组件块、图片粘贴/拖放、数学公式、表格、任务列表和类 Notion 的块控制。
- 同步刷新 Agent、Reader、Library、PDF 和共享 UI 流程，改进桌面集成和状态管理。

## [0.1.6] - 2026-05-19

### 新增

- 独立本地文库：PDF 导入、存储目录配置、自定义分类、标签、搜索、排序、收藏和阅读进度记录。
- 可选 Zotero 兼容：导入已有的分类、标签、元数据和本地 PDF 附件。
- 内置 PDF 阅读工作流：MinerU 结构化解析、块级联动定位、全文翻译、笔记和 AI 论文概览。
- 独立 Agent 工作区：对话历史、执行轨迹、工具调用卡片，以及元数据补全、打标签、分类、重命名等批量文献操作。
- 可配置 OpenAI 兼容模型，用于翻译、概览生成、问答和 Agent 任务；提供 Windows、macOS、Linux 桌面安装包。
- WebDAV 手动远程备份与增量恢复：SQLite 在线备份生成本地暂存副本，远端对象临时上传后原子切换，支持 latest 备份查看与设置页入口。

---

更早版本（0.1.1 ~ 0.1.5）的变更未单独成文，可从 [提交历史](https://github.com/Dongyurocket/PaperQuay/commits/main) 追溯。
